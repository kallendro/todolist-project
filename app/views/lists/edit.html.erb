<% form_for @list, :url => user_list_path(@user, @list), :html => {:method => :put} do |form| %>
  <%= form.label :subject %>
  <%= form.text_field :subject %>
  <%= form.submit "Update" %>
<% end %>
